#Config file for OpenOCD. Used for correct chip programming/debugging (if OpenOCD is used)

source [find interface/stlink.cfg]
source [find target/stm32f2x.cfg]

transport select hla_swd

adapter speed 950
